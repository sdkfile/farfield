{
  "name": "farfield",
  "private": true,
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:remote": "node scripts/dev.mjs --remote",
    "server": "bun run --filter @farfield/server dev:remote",
    "build": "bun run prepare:workspace-dist && bun run --workspaces build",
    "start": "bun run --parallel --filter @farfield/server --filter @farfield/web start",
    "start:prod": "bun run start",
    "test": "bun run --workspaces test",
    "lint": "bun run --workspaces lint",
    "typecheck": "bun run --workspaces typecheck",
    "prepare:workspace-dist": "bun run --filter @farfield/unified-surface build && bun run --filter @farfield/protocol build && bun run --filter @farfield/opencode-api build && bun run --filter @farfield/api build",
    "verify:no-cheats": "node scripts/verify-no-cheats.mjs",
    "verify:no-provider-ui-imports": "node scripts/verify-no-provider-imports-in-ui.mjs",
    "verify:codex-generated": "node scripts/verify-codex-generated-clean.mjs",
    "verify:opencode-generated": "node scripts/verify-opencode-generated-clean.mjs",
    "verify:strict": "bun run verify:no-cheats && bun run verify:no-provider-ui-imports && bun run prepare:workspace-dist && bun run typecheck && bun run test && bun run verify:codex-generated && bun run verify:opencode-generated",
    "sanitize:traces": "node scripts/sanitize-traces.mjs",
    "generate:codex-schema": "node scripts/generate-codex-schema.mjs",
    "generate:opencode-manifest": "node scripts/generate-opencode-manifest.mjs",
    "screenshot:codex": "node skills/codex-window-debug/tools/codex-window-screenshot.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
